import { DataRekeningKoranPost } from './data-rekening-koran-post';

describe('DataRekeningKoranPost', () => {
  it('should create an instance', () => {
    expect(new DataRekeningKoranPost()).toBeTruthy();
  });
});
